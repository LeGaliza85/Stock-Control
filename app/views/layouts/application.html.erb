<!DOCTYPE html>
<html>
<head>
  <title>StockControl</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body class="min-h-screen" style="background-color: var(--bg-cream); color: var(--text-primary); font-family: 'DM Sans', sans-serif;">
  <!-- Cabecera simple: logo a la izquierda, menú central y un botón de menú a la derecha -->
  <header>
    <div class="container mx-auto px-4 py-3" style="display:flex; align-items:center; justify-content:space-between;">
      <%= link_to root_path do %>
        <img src="/logo.webp" alt="StockControl" style="width:32px; height:32px;">
      <% end %>
      <nav style="flex:1; display:flex; justify-content:center; gap:28px;">
        <a href="<%= productos_path %>" style="color:#E8E0D4; text-decoration:none; font-size:14px;">Productos</a>
        <a href="<%= categorias_path %>" style="color:#E8E0D4; text-decoration:none; font-size:14px;">Categorías</a>
      </nav>
      <button class="md:hidden" aria-label="Open menu">☰</button>
    </div>
  </header>
  <main class="container mx-auto px-4 py-8">
    <%= yield %>
  </main>
</body>
</html>
